
  # Add prompt to file

  This is a code bundle for Add prompt to file. The original project is available at https://www.figma.com/design/N8jkyudHHAm3Eh0zgXhFWK/Add-prompt-to-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  